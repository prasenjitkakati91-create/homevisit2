import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.physiotrack.app',
  appName: 'PhysioTrack',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
